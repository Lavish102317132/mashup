# mashup
This project is a Python command line program that creates an audio mashup from YouTube videos of a given singer. It downloads videos, converts them to audio, extracts a fixed duration from each file, and merges them into a single output audio file.
